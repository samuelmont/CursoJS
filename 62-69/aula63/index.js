//               -5       -4       -3         -2        -1
//                0        1        2          3         4
const nomes = ['Maria', 'João', 'Eduardo', 'Gabriel', 'Júlia'];

console.log(nomes);

// nomes.splice(índice atual, delete, elem1, elem2, elem3);

// pop
// nomes.splice(-1, 1);

// Push
// nomes.splice(nomes.length, 0, 'Samuel');

// Unshift
// nomes.splice(0, 0, 'Samuel');

// shift
// nomes.splice(0, 1);